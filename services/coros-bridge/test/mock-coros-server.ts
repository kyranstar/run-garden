/** Shim: the mock moved to @rg/coros with the modules it fakes; bridge
 * suites that still need it import through this path unchanged. */
export * from "../../../packages/coros/test/mock-coros-server.js";
